//
//  Tokens.swift
//  CoreApplicationPrototype
//
//  Created by Cher Moua on 4/14/17.
//  Copyright © 2017 InboundRXCapstone. All rights reserved.
//

/*
import Foundation
import JWT


class Tokens: NSObject{
    
    let secret = "super random key"

    
    func decodeToken(tokenAuth: String) -> (String,ClaimSet?){
        do {
            let claims: ClaimSet = try JWT.decode(tokenAuth, algorithm: .hs256(secret.data(using: .utf8)!))
            //print("THIS IS THE CLAIM:")
            //print(claims)
            return ("Success", claims)
        } catch {
            print("Failed to decode JWT: \(error)")
            return ("Failure", nil)
        }
    }
}
 */
